#include "main.h"
#include <stdlib.h>
/**
 * _isupper - output uppercase letter
 * @c: parameter
 * Return: 1 if uppercase and 0 otherwise
 */
int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
